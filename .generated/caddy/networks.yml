# Generated by scaffold/docker/render-caddy-routes.sh; do not edit.
# Per-app proxy networks: Caddy joins all of them; each app joins only
# its own, so apps cannot reach each other or forge Remote-* headers.
services:
  caddy:
    networks:
      - ntfy_proxy
  ntfy:
    networks:
      - ntfy_proxy
networks:
  ntfy_proxy: {}
